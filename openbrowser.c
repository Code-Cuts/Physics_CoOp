#include "myglobals.h"

void openbrowser()
{
  TBrowser* pbrowser = new TBrowser();
  //while (!b->GetSelected());
  //(TPad*)(b->GetSelected())->AddExec("e", ".x exec4.c");
  //TPad* p = (TPad*)(b->GetSelected());
  //p->AddExec("E", ".x exec4.c");
  //TPad* pCanvas = (TPad*)(pbrowser->GetSelected());
  //pCanvas->AddExec("E", ".x exec4.c");
  //while(!Canvas_1);
  //Canvas_1->AddExec("E1", ".x exec4.c");
}
